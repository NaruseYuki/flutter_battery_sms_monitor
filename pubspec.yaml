name: flutter_battery_sms_monitor
description: A Flutter app to monitor battery level and read SMS messages, then post to Slack.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Battery monitoring (system_state from GitHub)
  system_state:
    git:
      url: https://github.com/ShamilPP/system_state.git
      ref: 466137ccc2ca2d68357ce061c2d53e01e9135bb2
  
  # Alarm manager for scheduled tasks
  android_alarm_manager_plus: ^4.0.3
  
  # SMS reading
  another_telephony: ^0.2.1
  
  # HTTP communication with Retrofit
  dio: ^5.4.0
  retrofit: ^4.0.3
  json_annotation: ^4.8.1
  
  # Shared preferences for storing settings
  shared_preferences: ^2.2.2
  
  # Permission handling
  permission_handler: ^11.1.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code generation for Retrofit
  build_runner: ^2.4.7
  retrofit_generator: ^8.0.6
  json_serializable: ^6.7.1
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
